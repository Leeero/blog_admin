import React from 'react'
import { useHistory } from 'react-router-dom'
import './index.scss'

export default function PageList() {
  const history = useHistory()

  return <div className="home">这是首页</div>
}
